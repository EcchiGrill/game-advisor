export enum OrderingValue {
  name = 'name',
  slug = 'slug',
  playtime = 'playtime',
  rating = 'rating',
  metacritic = 'metacritic',
  releasedAt = 'releasedAt',
  createdAt = 'createdAt',
  updatedAt = 'updatedAt',
}
